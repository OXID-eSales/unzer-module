{% set shop = oEmailView.getShop() %}
{% set oViewConf = oEmailView.getViewConfig() %}
{% block unzer_email_plain_cancel_intro %}
{% if isUnzerOwnerMail %}
{{ translate({ ident: "OSCUNZER_CANCEL_MAIL_INTRO_OWNER" }) }}
{% else %}
{{ translate({ ident: "OSCUNZER_CANCEL_MAIL_SALUTATION" }) }} {{ order.oxorder__oxbillfname.getRawValue() }} {{ order.oxorder__oxbilllname.getRawValue() }},

{{ translate({ ident: "OSCUNZER_CANCEL_MAIL_INTRO" }) }}
{% endif %}
{% endblock %}

{% block unzer_email_plain_cancel_details %}
{{ translate({ ident: "ORDER_NUMBER", suffix: "COLON" }) }} {{ order.oxorder__oxordernr.value }}
{{ translate({ ident: "OSCUNZER_CANCEL_MAIL_ORDER_TOTAL", suffix: "COLON" }) }} {{ format_price(order.oxorder__oxtotalordersum.value, { currency: currency }) }}
{% if unzerRefundedAmount is not null %}
{{ translate({ ident: "OSCUNZER_CANCEL_MAIL_REFUNDED", suffix: "COLON" }) }} {{ format_price(unzerRefundedAmount, { currency: currency }) }}
{% endif %}
{% endblock %}

{% block unzer_email_plain_cancel_note %}
{% if not isUnzerOwnerMail %}
{% if unzerRefundedAmount is not null %}
{{ translate({ ident: "OSCUNZER_REFUND_MAIL_NOTE" }) }}
{% else %}
{{ translate({ ident: "OSCUNZER_CANCEL_MAIL_NOTE_NO_REFUND" }) }}
{% endif %}
{% endif %}
{% endblock %}

{{ shop.oxshops__oxname.getRawValue() }}
{{ shop.oxshops__oxurl.value }}
